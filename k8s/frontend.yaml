apiVersion: apps/v1
kind: Deployment
metadata:
  name: casino-frontend
  labels:
    app: casino-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: casino-frontend
  template:
    metadata:
      labels:
        app: casino-frontend
    spec:
      containers:
        - name: frontend
          image: 995732577501.dkr.ecr.us-east-1.amazonaws.com/frontend-casino:latest
          ports:
            - containerPort: 80
          env:
            - name: BACKEND_HOST
              value: "casino-backend-service"
          resources:
            requests:
              cpu: "100m"
            limits:
              cpu: "200m"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: casino-frontend-service
spec:
  type: LoadBalancer
  selector:
    app: casino-frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80