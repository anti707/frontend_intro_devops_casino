apiVersion: v1
kind: Service
metadata:
  name: casino-frontend-lb
  labels:
    app: casino-frontend
spec:
  type: ClusterIP
  selector:
    app: casino-frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80